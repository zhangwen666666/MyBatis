<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zw.mybatis.mapper.ClazzMapper">
    <select id="selectByIdStep2" resultType="clazz">
        select cid,cname from t_clazz where cid = #{cid}
    </select>

    <resultMap id="clazzResultMapCollection" type="clazz">
        <id property="cid" column="cid"/>
        <result property="cname" column="cname"/>
        <collection property="students" ofType="student">
            <id property="sid" column="sid"/>
            <result property="sname" column="sname"/>
        </collection>
    </resultMap>
    <select id="selectByIdCollection" resultMap="clazzResultMapCollection">
        select
            c.cid,c.cname, s.sid, s.sname
        from
            t_clazz c
        left join
            t_stu s
        on
            c.cid = s.cid
        where
            c.cid = #{cid}
    </select>


    <resultMap id="clazzResultMapStep" type="clazz">
        <id property="cid" column="cid"/>
        <result property="cname" column="cname"/>
        <collection fetchType="lazy" select="com.zw.mybatis.mapper.StudentMapper.selectByCid" property="students" column="cid"/>
    </resultMap>
    <select id="selectByIdStep" resultMap="clazzResultMapStep">
        select * from t_clazz where cid = #{cid}
    </select>
</mapper>